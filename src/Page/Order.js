import React from 'react'
import { Card, Button, Alert, Tab, Col, Row, Nav } from "react-bootstrap"

export default function Order() {
    return (
        <div>
            Orders
        </div>
    )
}
